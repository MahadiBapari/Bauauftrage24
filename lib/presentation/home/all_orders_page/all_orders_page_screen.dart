import 'package:flutter/material.dart';

class AllOrdersPageScreen extends StatelessWidget {
  const AllOrdersPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'all order page',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}