import 'package:flutter/material.dart';

class AddNewOrderPageScreen extends StatelessWidget {
  const AddNewOrderPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'add new order page',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}